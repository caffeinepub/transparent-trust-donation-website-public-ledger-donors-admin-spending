{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Replace UPI QR static asset with newly uploaded image",
  "requirements": [
    {
      "id": "REQ-59",
      "summary": "Replace the existing UPI QR static asset file while keeping the same public path and all frontend references unchanged.",
      "acceptanceCriteria": [
        "`frontend/public/assets/generated/upi-qr.dim_800x800.jpg` is replaced with the newly uploaded QR image and is included in the deployed frontend build.",
        "No code changes are required to reference the QR image; `frontend/src/utils/upiQrImage.ts` still exports `'/assets/generated/upi-qr.dim_800x800.jpg'`.",
        "On the Donate flow, the QR image renders successfully in the UPI payment section without triggering the QR load error state.",
        "Clicking “Open QR Full Size” opens the same updated QR image in a new tab."
      ],
      "file_operations": [
        {
          "path": "frontend/public/assets/generated/upi-qr.dim_800x800.jpg",
          "operation": "modify",
          "description": "Replace the existing static asset with the newly uploaded UPI QR image, keeping the exact filename and location so `UPI_QR_IMAGE_PATH` (`/assets/generated/upi-qr.dim_800x800.jpg`) continues to work without any code changes."
        }
      ]
    }
  ]
}